export default defineAppConfig({
	ui: {
		input: {
			slots: {
				root: "w-full",
			},
		},
		textarea: {
			slots: {
				root: "w-full",
			},
		},
		select: {
			slots: {
				base: "w-full",
			},
		},
		inputMenu: {
			slots: {
				root: "w-full",
			},
		},
	},
});
